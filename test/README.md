# Test

Area riservata ai test di logica e validazione.

Priorita' consigliate:

- validazione `POST /api/config`
- validazione `POST /api/circuits`
- state machine runtime
- semantica `show/stop/reset/clear`
